Running any `npm run test` will automatically pick up `playwright.config.js`
If config file is in a different location, pass it with --config option
Example: {
  "scripts": {
    "test": "playwright test --config=tests/example.config.js"
  }
}

